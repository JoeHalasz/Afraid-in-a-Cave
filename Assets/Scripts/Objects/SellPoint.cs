using UnityEngine;

public class SellPoint : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        transform.position += transform.right * 3.5f;
    }

}
